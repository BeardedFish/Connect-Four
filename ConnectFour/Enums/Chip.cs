namespace ConnectFour.Enums
{
    /// <summary>
    /// An enum that holds all the possible results the game board can contain.
    /// </summary>
    public enum Chip
    {
        Empty,
        Red,
        Yellow
    }
}
